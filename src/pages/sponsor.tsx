import type { NextPage } from "next";

import Layout from "@components/Layout";

const Sponsor: NextPage = () => {
  return (
    <Layout title="Jetcap - You can sponsor this project">
      Hello world!
    </Layout>
  );
};

export default Sponsor;
